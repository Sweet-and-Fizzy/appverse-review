# MLflow Tracking Server

Run MLflow's experiment tracking UI as an interactive OOD app.

## Requirements

- Singularity images for nginx and MLflow (built from provided definitions)
- CSC OOD environment utilities (ood-util, ood-initializers)
